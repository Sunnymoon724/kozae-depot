# Copy to SitePath\<alias>.env
# <alias> = name from Projects.list (used by @direct / @common in ConsoleTargets.list)
# BuildConsole copies this once into each project Tool/Paths.env on first deploy.

PROJECTS_ROOT=
WORK_PROJECT_PATH=
DATA_ROOT_PATH=
PROTO_SOURCE_PATH=
