# Deploy destinations only (build/copy).
# alias = relative path under PROJECTS_ROOT
# Runtime tools must NOT read this file.

# Helper dll target (BuildHelper)
<HelperAlias>=<ProjectRoot>

# Console tool copy roots (BuildConsole + ConsoleTargets.list)
# Tool land under <alias>\<ToolPaths.map>
<ConsoleAlias>=<ProjectRoot>